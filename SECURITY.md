# Security

CrackShell is intended for authorized password recovery and defensive testing.

Do not submit real passwords, private hashes, encryption keys, confidential images, or wordlists in public issues. Use synthetic test data when reporting bugs.

The application should remain local-only. Any change that adds file uploads, telemetry, remote execution, or automatic network requests requires an explicit security review.
